package io.speejson;

public class Test {

	public Object read(Object obj, int idx) {
		
		Object ret = null;
		
		//if(idx == 0) ret = obj.getName();
		//if(idx == 1) ret = obj.getEmail();
		/*if(idx == 2) ret = obj.getId();
		if(idx == 3) ret = obj.getBirth();
		if(idx == 4) ret = obj.getAddress();
		if(idx == 5) ret = obj.getAge();
		if(idx == 6) ret = obj.isActive();
*/
		return ret;
	}
	
	

	
}
